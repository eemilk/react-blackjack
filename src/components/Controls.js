import React from 'react';
import '.././index.css';

const Controls = (props) => {
    console.log("this is controls");
    return(
        <div>
            <button>Deal</button>
            <button>Hit</button>
            <button>Stand</button>
        </div>
    )
}

export default Controls;